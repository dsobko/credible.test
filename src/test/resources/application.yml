logging:
  level:
    org.springframework.web: 'INFO'
  pattern:
    console: "%date %5level [%thread] -- %-40.40logger{39} {%mdc{scenarioName}}: %msg%n%ex"

credible:
  service:
    host: ${CREDIBlE_HOST:https://www.credible.com//?optimizely_disable=true}

chromedriver:
  path: C:/Temp/chromedriver_win32/chromedriver.exe

common:
  properties:
    amountToLoan: 15000


